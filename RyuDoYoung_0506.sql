CREATE TABLE SCORE_BOX(
STU_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
STU_NAME VARCHAR2(50) NOT NULL,
KOR NUMBER CHECK(0<=KOR OR KOR<=100),
ENG NUMBER CHECK(0<=ENG OR ENG<=100),
MATH NUMBER CHECK(0<=MATH OR MATH<=100),
SOCI NUMBER CHECK(0<=SOCI OR SOCI<=100),
SCIN NUMBER CHECK(0<=SCIN OR SCIN<=100),
TOTAL NUMBER,
AGV NUMBER,
RANK NUMBER
);

COMMIT;

DROP TABLE SCORE_BOX;

delete from score_box;
